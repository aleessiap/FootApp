Restore the database via pgadmin, importing and running the db_structure.sql file to use the local version.
If the db is not local, edit the server.js file and change the connection settings.

Open the project and run from the terminal, in order, the following commands:
1)yarn install inside the folder test-speech.
2)yarn install inside the client folder.
3)yarn dev inside test-speech folder.