import {Record} from "immutable";

/**
 * Valori di default dello store Login
 */
const LoginDefault = Record({
    id:'',
    username:'',
    nome:'',
    cognome:''
});

export default LoginDefault;
